server:
  port: ${SERVER_PORT:8083}

payment:
  behaviour: ${PAYMENT_BEHAVIOUR:SUCCESS}
  callback-secret: ${PAYMENT_CALLBACK_SECRET:local-development-callback-secret}
  callback-delay-ms: ${PAYMENT_CALLBACK_DELAY_MS:750}
  timeout-ms: ${PAYMENT_TIMEOUT_MS:5000}

claims:
  callback-url: ${CLAIMS_CALLBACK_URL:http://localhost:8080/internal/v1/payment-status-events}
