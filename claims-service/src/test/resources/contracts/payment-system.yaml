openapi: 3.0.3
info:
  title: Payment System request API
  version: 1.0.0
paths:
  /api/v1/payment-requests:
    post:
      operationId: createPayment
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema: {type: string}
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentRequest'
      responses:
        '202':
          description: Payment accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentAccepted'
  /api/v1/payments/by-claim/{claimId}:
    get:
      operationId: findPaymentByClaim
      parameters:
        - in: path
          name: claimId
          required: true
          schema: {type: string, format: uuid}
      responses:
        '200':
          description: Current payment result used for reconciliation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentAccepted'
        '404':
          description: No payment exists for the claim
components:
  schemas:
    PaymentRequest:
      type: object
      required: [claimId, amount, currency]
      properties:
        claimId: {type: string, format: uuid}
        amount: {type: number}
        currency: {type: string, minLength: 3, maxLength: 3}
    PaymentAccepted:
      type: object
      required: [paymentReference, status]
      properties:
        paymentReference: {type: string}
        status: {type: string}
